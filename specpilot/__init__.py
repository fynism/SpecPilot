"""SpecPilot 的顶层 Python 包。

当前职责：
    标识 ``specpilot`` 为一个可导入的软件包。具体能力分别位于 Agent Loop、工具、
    Hook、安全策略和 CLI 模块，避免在包导入阶段隐式启动程序。

后续扩展：
    当项目形成稳定公共 API 后，可以在这里集中导出少量核心类型和版本号；不要把
    所有内部实现都重新导出，以免模块边界失去意义。
"""
