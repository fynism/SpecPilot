"""SpecPilot 的顶层 Python 包。

当前职责：
    标识 ``specpilot`` 为一个可导入的软件包。运行时、工具、Specification 与 Eval
    分别位于对应子包；CLI 和配置保留在顶层作为组合入口。

后续扩展：
    当项目形成稳定公共 API 后，可以在这里集中导出少量核心类型和版本号；不要把
    所有内部实现都重新导出，以免模块边界失去意义。
"""
